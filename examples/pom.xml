<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>dev.apitestkit</groupId>
    <artifactId>spring-api-testkit</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>

  <artifactId>examples</artifactId>
  <name>examples</name>
  <description>Voorbeeldspec, voorbeeldconfiguratie en WireMock-integratietests</description>

  <dependencies>
    <dependency>
      <groupId>dev.apitestkit</groupId>
      <artifactId>spec-core</artifactId>
    </dependency>
    <dependency>
      <groupId>dev.apitestkit</groupId>
      <artifactId>test-runner</artifactId>
    </dependency>
    <dependency>
      <groupId>dev.apitestkit</groupId>
      <artifactId>codegen</artifactId>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
    </dependency>
    <dependency>
      <groupId>io.rest-assured</groupId>
      <artifactId>rest-assured</artifactId>
    </dependency>
    <dependency>
      <groupId>org.wiremock</groupId>
      <artifactId>wiremock-standalone</artifactId>
    </dependency>
    <dependency>
      <groupId>org.slf4j</groupId>
      <artifactId>slf4j-simple</artifactId>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <!-- generated/** en AdHocApiTest verwachten een echt draaiende API (zie docs/UITLEG.md)
               en worden daarom niet automatisch meegedraaid in mvn test/verify. Compilatie wordt
               wel altijd gecontroleerd. -->
          <excludes>
            <exclude>generated/**</exclude>
            <exclude>**/AdHocApiTest.java</exclude>
          </excludes>
        </configuration>
      </plugin>
      <plugin>
        <!-- Zet target/allure-results om in een HTML-rapport: `mvn allure:report` (bestand in
             target/site/allure-maven-plugin/index.html) of `mvn allure:serve` (opent meteen in de
             browser). Downloadt zelf de Allure command line tool, dus je hoeft niets te installeren.
             Werkt alleen als het profiel allure-reporting actief is (standaard wel, zie
             test-runner/pom.xml) en je mirror io.qameta.allure kan leveren. -->
        <groupId>io.qameta.allure</groupId>
        <artifactId>allure-maven</artifactId>
        <version>2.15.2</version>
        <configuration>
          <reportVersion>2.29.0</reportVersion>
        </configuration>
      </plugin>
      <plugin>
        <!-- Alternatief voor Allure zonder extra externe artefacten: leest de Surefire-XML-bestanden
             die er toch al staan en zet ze om in één leesbare pagina. Draai `mvn surefire-report:report-only`
             (bestand verschijnt op target/reports/surefire.html). Werkt ook als Allure niet
             beschikbaar is op jouw Maven-mirror (zie test-runner/pom.xml, profiel allure-reporting). -->
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-report-plugin</artifactId>
        <version>${surefire.version}</version>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>generate</id>
      <build>
        <plugins>
          <plugin>
            <groupId>org.codehaus.mojo</groupId>
            <artifactId>exec-maven-plugin</artifactId>
            <version>3.4.1</version>
            <configuration>
              <mainClass>dev.apitestkit.codegen.CodegenMain</mainClass>
              <classpathScope>test</classpathScope>
            </configuration>
            <executions>
              <execution>
                <id>generate-tests</id>
                <phase>generate-test-sources</phase>
                <goals>
                  <goal>java</goal>
                </goals>
              </execution>
            </executions>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>
</project>
