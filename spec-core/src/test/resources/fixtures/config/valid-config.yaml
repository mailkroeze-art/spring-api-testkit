baseUrl: http://localhost:8080
auth:
  type: bearer
  tokenEnv: API_TOKEN
operations:
  createPet:
    skip: false
    expectedStatusOverrides:
      MISSING_REQUIRED: 422
    fixedTestData:
      name: "Fido"
  getPetById:
    setupDependsOn: createPet
caseTypes:
  UNAUTHORIZED: false
